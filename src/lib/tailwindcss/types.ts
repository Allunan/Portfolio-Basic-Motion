export interface GridVars {
  maxWidth?: number

  desktop: {
    gap: number
    columns: number
    margins: number
  }

  tablet: {
    gap: number
    columns: number
    margins: number
  }

  phone: {
    gap: number
    columns: number
    margins: number
  }
}
